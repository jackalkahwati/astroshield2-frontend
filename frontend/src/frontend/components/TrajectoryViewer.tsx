import React from 'react';
import Box from '@mui/material/Box';

const TrajectoryViewer: React.FC = () => {
  return (
    <Box sx={{ border: '1px dashed grey', padding: 2, minHeight: 300 }}>
      Trajectory Viewer Placeholder
    </Box>
  );
};

export default TrajectoryViewer; 