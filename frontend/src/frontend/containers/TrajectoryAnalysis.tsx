import React from 'react';
import Box from '@mui/material/Box';
import TrajectoryViewer from '../components/TrajectoryViewer'; // Keep the import if needed by page

const TrajectoryAnalysis: React.FC = () => {
  return (
    <Box sx={{ border: '1px dashed grey', padding: 2 }}>
      Trajectory Analysis Container Placeholder
      {/* Optionally include the viewer placeholder if the page structure needs it */}
      {/* <TrajectoryViewer /> */}
    </Box>
  );
};

export default TrajectoryAnalysis; 